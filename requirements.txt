# Core dependencies
pytubefix
pydub
librosa
requests
watchdog
rich
python-dotenv
Pillow
colorthief
beautifulsoup4
rapidfuzz
stable-whisper
plyer
win10toast

# Database
sqlite3

# Optional/test dependencies
unittest

# VS Code extensions (recommended for development)
# These are not Python packages, but can be installed via VS Code Marketplace
# - ms-python.python (Python support)
# - ms-toolsai.jupyter (Jupyter notebook support)
# - ms-python.vscode-pylance (Pylance language server)

# Note: VS Code extensions should be installed via VS Code, not pip.
