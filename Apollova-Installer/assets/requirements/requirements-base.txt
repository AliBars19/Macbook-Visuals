# Apollova Base Requirements — pinned versions for stability
# Setup.exe will enforce these exact versions, uninstalling conflicting ones first.

# Audio processing
pytubefix==8.12.2
pydub==0.25.1
librosa==0.10.2

# Transcription (torch installed separately by setup)
openai-whisper==20240930
stable-ts==2.17.4

# Lyrics
lyricsgenius==3.0.1
rapidfuzz==3.9.7

# Image processing
colorthief==0.2.1
Pillow==10.4.0
requests==2.32.3

# Numeric (must be <2 for torch compatibility)
numpy==1.26.4

# Environment / config
python-dotenv==1.0.1

# GUI
PyQt6==6.7.1
